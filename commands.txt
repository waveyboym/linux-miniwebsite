//starting from root of folder project 
source ./run <to run everything>
source ./close <to close and cleanup everything>

to regenerate reuqirements.txt file(starting from root of folder project ):
1. source miniwebsite_venv/bin/activate
2. pip freeze > requirements.txt


deactivate-virtual-environment: deactivate
activate-virtual-environment: source miniwebsite_venv/bin/activate <must be executed in "/mnt/c/personal projects/linux-miniwebsite$">
run server: python manage.py runserver
run server w/ gunicorn: gunicorn miniwebsite.wsgi --timeout 60



//build image<dev>
docker-compose build

//run container<dev>
docker-compose up -d

//stop container<dev>
docker-compose stop

//bringing down dev container<prod>
docker-compose down -v

//build prod image
docker-compose -f docker-compose.prod.yml up -d --build